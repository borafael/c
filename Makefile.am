# Makefile.am (Root Directory)
ACLOCAL_AMFLAGS = -I m4

# Order matters: build the library first, then the apps!
SUBDIRS = libs/math libs/thread libs/raytrace libs/slice libs/battleforge apps/nbody apps/rtdemo apps/barrier
